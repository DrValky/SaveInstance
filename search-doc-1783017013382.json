{"searchDocs":[{"title":"SynSaveInstance","type":0,"sectionRef":"#","url":"/SaveInstance420Edition/api/SynSaveInstance","content":"","keywords":"","version":null},{"title":"Types​","type":1,"pageTitle":"SynSaveInstance","url":"/SaveInstance420Edition/api/SynSaveInstance#types","content":" ","version":null,"tagName":"h2"},{"title":"CustomOptions table​","type":1,"pageTitle":"SynSaveInstance","url":"/SaveInstance420Edition/api/SynSaveInstance#CustomOptions table","content":"&lt;/&gt; interface CustomOptions table { __DEBUG_MODE: boolean-- This will print debug logs to console about unusual scenarios. Recommended to enable if you wish to help us improve our products and find bugs / issues with it! Default: false ReadMe: boolean-- Default: true SafeMode: boolean-- Kicks you before Saving, which prevents you from being detected in any game. Default: false ShutdownWhenDone: boolean-- Shuts the game down after saveinstance is finished. Default: false AntiIdle: boolean-- Prevents the 20-minute-Idle Kick. Default: true ShowStatus: boolean-- Default: true Callback: function-- If set, the serialized data will be sent to the callback function instead of to file. Default: false mode: string-- Valid modes: full, optimized, scripts. Change this to invalid mode like &quot;invalid&quot; if you only want ExtraInstances. &quot;optimized&quot; mode is NOT supported with @Object option. Default: &quot;optimized&quot; noscripts: boolean-- Aliases: Decompile. Default: false scriptcache: boolean-- Default: true decomptype: string-- Deprecated. Default: Uses your executor's decompiler, if available. timeout: number-- If the decompilation run time exceeds this value it gets cancelled. Set to -1 to disable timeout (unreliable). Aliases: DecompileTimeout. Default: 10 DecompileJobless: boolean-- Includes already decompiled code in the output. No new scripts are decompiled. Default: false SaveBytecode: boolean-- Includes bytecode in the output. Useful if you wish to be able to decompile it yourself later. Default: false DecompileIgnore: {Instance | Instance.ClassName | [Instance.ClassName]={Instance.Name}}-- Ignores match &amp; it's descendants by default. To Ignore only the instance itself set the value to = false. Examples: &quot;Chat&quot;, - Matches any instance with &quot;Chat&quot; ClassName, Players = {&quot;MyPlayerName&quot;} - Matches &quot;Players&quot; Class AND &quot;MyPlayerName&quot; Name ONLY, workspace - matches Instance by reference, [workspace] = false - matches Instance by reference and only ignores the instance itself and not it's descendants. Default: {TextChatService} IgnoreList: {Instance | Instance.ClassName | [Instance.ClassName]={Instance.Name}}-- Structure is similar to @DecompileIgnore except = false meaning if you ignore one instance it will automatically ignore it's descendants. Default: {CoreGui, CorePackages} ExtraInstances: {Instance }-- If used with any invalid mode (like &quot;invalidmode&quot;) it will only save these instances. Default: {} IgnoreProperties: table-- Ignores properties by Name. Default: {} SaveCacheInterval: number-- The less the value the more often it saves, but that would mean less performance due to constantly saving. Default: 0x1600 * 10 FilePath: string-- Must only contain the name of the file, no file extension. Default: false Object: Instance -- If provided, saves as .rbxmx (Model file) instead. If Object is game, it will be saved as a .rbxl file. MUST BE AN INSTANCE REFERENCE, FOR EXAMPLE - game.Workspace. &quot;optimized&quot; mode is NOT supported with this option. If IsModel is set to false then Object specified here will be saved as a place file. Default: false IsModel: boolean-- If Object is specified then sets to true automatically, unless you set it to false. Default: false NilInstances: boolean-- Save instances that aren't Parented (Parented to nil). Default: false NilInstancesFixes: {[Instance.ClassName]=function}-- This can cause some Classes to be fixed even though they might not need the fix (better be safe than sorry though). For example, Bones inherit from Attachment if we dont define them in the NilInstancesFixes then this will catch them anyways. TO AVOID THIS BEHAVIOR USE THIS EXAMPLE: {ClassName_That_Doesnt_Need_Fix = false}. Default: {Animator = function, AdPortal = function, BaseWrap = function, Attachment = function} IgnoreDefaultProperties: boolean-- Ignores default properties during saving. Default: true IgnoreNotArchivable: boolean-- Ignores the Archivable property and saves Non-Archivable instances. Default: true IgnorePropertiesOfNotScriptsOnScriptsMode: boolean-- Ignores property of every instance that is not a script in &quot;scripts&quot; mode. Default: false IgnoreSpecialProperties: boolean-- Prevents calls to gethiddenproperty and uses fallback methods instead. This also helps with crashes. If your file is corrupted after saving, you can try turning this on. Default: false UseUGCValidationService: boolean-- When false, the script never uses UGCValidationService:GetPropertyValue as a hidden-property fallback (some executors flag or lack it). gethiddenproperty is still used; only the UGC fallback is disabled. Default: true IsolateLocalPlayer: boolean-- Saves Children of LocalPlayer as separate folder and prevents any instance of ClassName Player with .Name identical to LocalPlayer.Name from saving. Default: false IsolateStarterPlayer: boolean-- If enabled, StarterPlayer will be cleared and the saved starter player will be placed into folders. Default: false IsolateLocalPlayerCharacter: boolean-- Saves Children of LocalPlayer.Character as separate folder and prevents any instance of ClassName Player with .Name identical to LocalPlayer.Name from saving. Default: false RemovePlayerCharacters: boolean-- Ignore player characters while saving. (Enables SaveNotCreatable automatically). Default: true SaveNotCreatable: boolean-- Includes non-serializable instances as Folder objects (Name is misleading as this is mostly a fix for certain NilInstances and isn't always related to NotCreatable). Default: false NotCreatableFixes: table&lt;Instance.ClassName&gt;-- {&quot;Player&quot;} is the same as {Player = &quot;Folder&quot;}; Format like {SpawnLocation = &quot;Part&quot;} is only to be used when SpawnLocation inherits from &quot;Part&quot; AND &quot;Part&quot; is Creatable. Default: { &quot;&quot;, &quot;Player&quot;, &quot;PlayerScripts&quot;, &quot;PlayerGui&quot;, &quot;TouchTransmitter&quot; } IsolatePlayers: boolean-- This option does save players, it's just they won't show up in Studio and can only be viewed through the place file code (in text editor). More info at https://github.com/luau/UniversalSynSaveInstance/issues/2. Default: false AlternativeWritefile: boolean-- Splits file content string into segments and writes them using appendfile. This might help with crashes when it starts writing to file. Though there is a risk of appendfile working incorrectly on some executors. Default: true IgnoreDefaultPlayerScripts: boolean-- **RISKY: Ignores Default PlayerScripts like PlayerModule &amp; RbxCharacterSounds. Prevents crashes on certain Executors. Default: true IgnoreSharedStrings: boolean-- RISKY: FIXES CRASHES (TEMPORARY, TESTED ON ROEXEC ONLY). FEEL FREE TO DISABLE THIS TO SEE IF IT WORKS FOR YOU. Default: true SharedStringOverwrite: boolean-- RISKY: if the process is not finished aka crashed then none of the affected values will be available. SharedStrings can also be used for ValueTypes that aren't SharedString, this behavior is not documented anywhere but makes sense (Could create issues though, due to potential ValueType mix-up, only works on certain types which are all base64 encoded so far). Reason: Allows for potential smaller file size (can also be bigger in some cases). Default: false TreatUnionsAsParts: boolean-- RISKY: Converts all UnionOperations to Parts. Useful if your Executor isn't able to save (read) Unions, because otherwise they will be invisible. Default: false (except Solara) SetStreaming: boolean-- Before saving, force-loads an entire StreamingEnabled map into memory so saveinstance captures the WHOLE map instead of only the chunks near you. Pins loaded content (ModelStreamingMode = Persistent) so nothing streams back out, then sweeps a grid with RequestStreamAroundAsync. Can be slow and laggy on large maps. Aliases: Streaming. Default: false StreamingAreaSize: number-- Side length (studs) of the square area swept around your starting position when SetStreaming is on. Default: 10000 StreamingRadius: number-- Per-request stream radius. Auto-uses the game's StreamingTargetRadius when readable. Default: 1024 StreamingTimeout: number-- Seconds to wait per RequestStreamAroundAsync request. Default: 20 StreamingConcurrency: number | boolean-- How many stream requests are kept in flight at once (higher = faster, more load). false = auto-scale to the grid size. Default: false (auto) StreamingSlices: number-- Vertical layers swept per grid cell; raise for tall maps. Default: 2 StreamingMaxTime: number | boolean-- Overall seconds cap for the streaming sweep. It gives up on stuck/empty chunks (common on ocean-heavy maps where RequestStreamAroundAsync hangs) and proceeds. false = auto-scale to the grid size (bigger maps get a longer cap, 60s–15m). Set a number to force it. Default: false (auto) StreamingChunkWait: number-- Max seconds to wait for one chunk to finish loading. The sweep waits until a region stops gaining parts (stability detection), capped here — dense regions load fully, empty ones return fast. Default: 12 StreamingSettleTime: number-- Seconds to wait after the streaming sweep for in-flight background terrain/loads to finish before saving. Also caps the post-sweep terrain-stabilization poll (waits until SmoothGrid stops growing) so terrain fills in instead of leaving floating islands. Default: 5 NeutralizeLighting: boolean-- Reset Lighting (ClockTime/Fog/Ambient), Atmosphere, post-effects (ColorCorrection/Bloom/Blur/SunRays/DoF) and clouds to clean midday right before saving, so the place opens BRIGHT instead of inheriting the game's dark/foggy/night atmosphere (the &quot;all grey on the camera&quot; look). false keeps the game's real lighting. Default: false ExportObj: boolean-- Also write a world-space .obj of all MeshPart geometry next to the save. Geometry is recovered with AssetService:CreateEditableMeshAsync, which works even for PRIVATE game meshes that Studio can't re-download (those save as grey boxes). The .obj (with per-part vertex colors) opens in Blender or Studio's 3D importer. MeshParts can't be made to render inside the place file itself, so this is the only way to recover their visuals. Best paired with SetStreaming (load the whole map first). World-space baking duplicates reused meshes, so big maps produce large files. Default: false Debug: boolean-- Writes a detailed run log to saveinstance-debug.txt in the executor workspace (executor, capabilities, options, streaming/prepass/decompile stats, save result). Share it for diagnosis. Default: false DecompilePrepass: boolean-- RISKY: Before saving, decompiles every client script in parallel via an external API (api.lua.expert) and pre-fills the decompile cache, so the save's decompile step is near-instant. Big speedup on script-heavy games. Sends script bytecode to a third-party API; needs getscriptbytecode + an HTTP request function. Default: false PrepassConcurrency: number-- Parallel decompile requests kept in flight during the prepass. Default: 24 PrepassRateGap: number-- Minimum seconds between decompile-API requests (lua.expert rejects unthrottled bursts). Lower = faster if the API allows. Default: 0.12 PrepassApiUrl: string-- Endpoint used by the decompile prepass. Default: &quot;https://api.lua.expert/decompile&quot; PrepassMaxScripts: number-- Upper bound for DecompilePrepass. If the game has more than this many client scripts, the prepass is skipped and scripts decompile on-demand during the save instead. The gather is cheap, the workers are throttled (reading thousands of scripts' bytecode no longer bursts/crashes), and decompiles are API-rate-limited, so this just avoids an absurdly long prepass on games so large the prepass would risk destabilizing an already memory-heavy client (e.g. Grow a Garden 2 with 25k+ scripts); those fall back to the stable on-demand decompile. Default: 6000 } Structure of the main CustomOptions table. Note: Options are case-insensitive, meaning you could type NilInstances option as nilInStaNces and it would still be valid. Anonymous {boolean|table{UserId = string, Name = string}} -- * RISKY: Cleans the file of any info related to your account like: Name, UserId. This is useful for some games that might store that info in GUIs or other Instances. Might potentially mess up parts of strings that contain characters that match your Name or parts of numbers that match your UserId. Can also be a table with UserId &amp; Name keys. Default: false   ","version":null,"tagName":"h3"},{"title":"OptionsAliases​","type":1,"pageTitle":"SynSaveInstance","url":"/SaveInstance420Edition/api/SynSaveInstance#OptionsAliases","content":"&lt;/&gt; interface OptionsAliases { FilePath: string-- FileName IgnoreDefaultProperties: string-- IgnoreDefaultProps SaveNotCreatable: string-- SaveNonCreatable IsolatePlayers: string-- SavePlayers scriptcache: string-- DecompileJobless timeout: string-- DecompileTimeout IgnoreNotArchivable: string-- IgnoreArchivable RemovePlayerCharacters: string-- INVERSE SavePlayerCharacters } Aliases for the SynSaveInstance.CustomOptions table. ","version":null,"tagName":"h3"},{"title":"Functions​","type":1,"pageTitle":"SynSaveInstance","url":"/SaveInstance420Edition/api/SynSaveInstance#functions","content":" ","version":null,"tagName":"h2"},{"title":"saveinstance​","type":1,"pageTitle":"SynSaveInstance","url":"/SaveInstance420Edition/api/SynSaveInstance#saveinstance","content":"This is a yielding function. When called, it will pause the Lua thread that called the function until a result is ready to be returned, without interrupting other scripts. Yields &lt;/&gt; SynSaveInstance.saveinstance( Parameter_1: variant&lt;table,table&lt;Instance &gt;&gt;,-- Can either be SynSaveInstance.CustomOptions table or a filled with instances ({Instance}), (then it will be treated as ExtraInstances with an invalid mode and IsModel will be true). Parameter_2: table-- [OPTIONAL] If present, then Parameter_2 will be assumed to be SynSaveInstance.CustomOptions table. And then if the Parameter_1 is an Instance, then it will be assumed to be SynSaveInstance.CustomOptions table.Object. If Parameter_1 is a table filled with instances ({Instance}), then it will be assumed to be SynSaveInstance.CustomOptions table.ExtraInstances and IsModel will be true). This exists for sake compatibility with saveinstance(game, {}) ) → () Saves instances with specified options. Example: local Params = { RepoURL = &quot;https://raw.githubusercontent.com/RealSlimShady2000/SaveInstance420Edition/main/&quot;, SSI = &quot;saveinstance&quot;, } local synsaveinstance = loadstring(game:HttpGet(Params.RepoURL .. Params.SSI .. &quot;.luau&quot;, true), Params.SSI)() local CustomOptions = { SafeMode = true, timeout = 15, SaveBytecode = true } synsaveinstance(CustomOptions)  ","version":null,"tagName":"h3"},{"title":"Getting Started","type":0,"sectionRef":"#","url":"/SaveInstance420Edition/docs/intro","content":"","keywords":"","version":"Next"},{"title":"Usage​","type":1,"pageTitle":"Getting Started","url":"/SaveInstance420Edition/docs/intro#usage","content":" local Params = { RepoURL = &quot;https://raw.githubusercontent.com/RealSlimShady2000/SaveInstance420Edition/main/&quot;, SSI = &quot;saveinstance&quot;, } local synsaveinstance = loadstring(game:HttpGet(Params.RepoURL .. Params.SSI .. &quot;.luau&quot;, true), Params.SSI)() synsaveinstance({ SetStreaming = false, -- true = capture the whole streaming map DecompilePrepass = false, -- true = decompile all scripts fast, up front NeutralizeLighting = false, -- true = save opens in bright daylight ExportObj = false, -- true = also dump meshes to a .obj })   One call, no extra scripts. Every option is off by default — flip on what you want.  ","version":"Next","tagName":"h2"},{"title":"Why use this one​","type":1,"pageTitle":"Getting Started","url":"/SaveInstance420Edition/docs/intro#why-use-this-one","content":" 🎨 Colors &amp; sizes save right (others load everything grey and default-sized)🖼️ Decals, images &amp; sounds save (the stuff most savers miss)🧩 Unions &amp; terrain render properly🗺️ Grabs the whole streaming map, not just what's near you📜 Decompiles scripts even if your executor can't☀️ Opens bright instead of dark and foggy🧱 Recovers meshes to .obj⚡ Fast and won't crash on huge games  👉 See the Options Reference for every setting.  Built on the original UniversalSynSaveInstance. Modified by Robloxscripts.com — Discord: discord.robloxscripts.com ","version":"Next","tagName":"h2"},{"title":"Options Reference","type":0,"sectionRef":"#","url":"/SaveInstance420Edition/docs/options","content":"","keywords":"","version":"Next"},{"title":"Most common​","type":1,"pageTitle":"Options Reference","url":"/SaveInstance420Edition/docs/options#most-common","content":" Option\tDefault\tWhat it doesmode\t&quot;optimized&quot;\t&quot;full&quot;, &quot;optimized&quot;, or &quot;scripts&quot;. &quot;optimized&quot; is smallest; use &quot;full&quot; for maximum fidelity. noscripts\tfalse\tSkip decompiling — save the map &amp; structure only (scripts kept as empty instances). SafeMode\tfalse\tKicks you from the game right before saving (anti-detection). ShowStatus\ttrue\tShow the on-screen progress bar. Debug\tfalse\tWrite saveinstance-debug.txt (executor, capabilities, stats) — send it for troubleshooting. FilePath\t(auto)\tOutput file name (no extension). Defaults to place &lt;id&gt; &lt;name&gt;. Callback\tfalse\tReceive the serialized string in a function instead of writing a file.  ","version":"Next","tagName":"h2"},{"title":"Map streaming — capture the whole map​","type":1,"pageTitle":"Options Reference","url":"/SaveInstance420Edition/docs/options#map-streaming--capture-the-whole-map","content":" Big StreamingEnabled maps only load chunks near you. These force-load the entire map first.  Option\tDefault\tWhat it doesSetStreaming\tfalse\tForce-load the whole StreamingEnabled map before saving (alias: Streaming). StreamingAreaSize\t10000\tSide length (studs) of the swept area. Raise for maps wider than 10k studs. StreamingConcurrency\tfalse (auto)\tRequests in flight at once. Auto-scales to map size; set a number to force. StreamingMaxTime\tfalse (auto)\tOverall time cap for the sweep. Auto-scales (60s–15m); never hangs. StreamingChunkWait\t12\tMax seconds to wait for one chunk (stability detection returns sooner). StreamingSettleTime\t5\tWait after the sweep for terrain/background loads to finish (fixes floating-island terrain). StreamingRadius\t1024\tPer-request radius (auto-uses the game's StreamingTargetRadius). StreamingSlices\t2\tVertical layers per grid cell; raise for tall maps.  ","version":"Next","tagName":"h2"},{"title":"Visuals & mesh recovery​","type":1,"pageTitle":"Options Reference","url":"/SaveInstance420Edition/docs/options#visuals--mesh-recovery","content":" Option\tDefault\tWhat it doesNeutralizeLighting\tfalse\tReset Lighting/Atmosphere/effects to clean midday so the place opens bright, not dark/foggy. ExportObj\tfalse\tAlso bake all MeshPart geometry to a world-space .obj (opens in Blender) — the only way to recover private meshes that Studio can't re-download. Best with SetStreaming. TreatUnionsAsParts\tfalse¹\tConvert Unions to plain Parts (for executors that can't read union mesh data). ¹true on Solara.  ","version":"Next","tagName":"h2"},{"title":"Decompiling scripts​","type":1,"pageTitle":"Options Reference","url":"/SaveInstance420Edition/docs/options#decompiling-scripts","content":" If your executor has no decompiler, scripts fall back to the free lua.expert API automatically.  Option\tDefault\tWhat it doesDecompilePrepass\tfalse\tDecompile every script in parallel via lua.expert before saving — big speedup on script-heavy games. Sends bytecode to a third-party API. PrepassConcurrency\t24\tParallel decompile requests during the prepass. PrepassRateGap\t0.12\tMin seconds between API requests (lower = faster if the API allows). PrepassApiUrl\tapi.lua.expert/decompile\tDecompile API endpoint. timeout\t10\tPer-script decompile timeout in seconds (alias: DecompileTimeout). SaveBytecode\tfalse\tAlso embed each script's bytecode in the output. DecompileJobless\tfalse\tOnly include already-decompiled code; decompile nothing new.  ","version":"Next","tagName":"h2"},{"title":"Choosing what to save​","type":1,"pageTitle":"Options Reference","url":"/SaveInstance420Edition/docs/options#choosing-what-to-save","content":" Option\tDefault\tWhat it doesObject\tfalse\tSave a specific instance (e.g. game.Workspace) as .rbxmx instead of the whole place. IsModel\tfalse\tForce model (.rbxmx) vs place (.rbxlx) output. IgnoreList\t{CoreGui, CorePackages}\tInstances/classes to skip (and their descendants). ExtraInstances\t{}\tSave only these instances (use with an invalid mode). IgnoreProperties\t{}\tSkip specific properties by name. IgnoreDefaultProperties\ttrue\tOmit properties equal to their class default (smaller files). IgnoreNotArchivable\ttrue\tSave instances even if Archivable = false. NilInstances\tfalse\tAlso save instances parented to nil.  ","version":"Next","tagName":"h2"},{"title":"Player isolation​","type":1,"pageTitle":"Options Reference","url":"/SaveInstance420Edition/docs/options#player-isolation","content":" Option\tDefault\tWhat it doesRemovePlayerCharacters\ttrue\tSkip player characters entirely. IsolateLocalPlayer\tfalse\tSave your player's children into a separate folder (not as a live Player). IsolateLocalPlayerCharacter\tfalse\tSame, for your character. IsolateStarterPlayer\tfalse\tClear StarterPlayer and folder-ize the saved starter player. IsolatePlayers\tfalse\tSave players but hidden (viewable only in the file text).  ","version":"Next","tagName":"h2"},{"title":"Stability & crash fixes (advanced / risky)​","type":1,"pageTitle":"Options Reference","url":"/SaveInstance420Edition/docs/options#stability--crash-fixes-advanced--risky","content":" Try these only if a save crashes or corrupts.  Option\tDefault\tWhat it doesIgnoreSpecialProperties\tfalse\tSkip gethiddenproperty calls (helps crashes, but breaks Unions/Terrain). IgnoreSharedStrings\ttrue\tDrop SharedStrings (crash fix). Union geometry is whitelisted so it still saves. UseUGCValidationService\ttrue\tSet false to never use UGCValidationService as a hidden-property fallback. AlternativeWritefile\ttrue\tWrite the file in segments (helps write-time crashes). IgnoreDefaultPlayerScripts\ttrue\tSkip PlayerModule/RbxCharacterSounds (crash fix on some executors). SaveCacheInterval\t~57k\tHow often (bytes) to flush to file — lower = safer but slower.  Full machine-generated reference (types, aliases, every field): SynSaveInstance API → ","version":"Next","tagName":"h2"}],"options":{"id":"default"}}